#include <bits/stdc++.h>
using namespace std;

int main() {
  int H, W, N;
  vector<int> x(H);
  vector<int> y(W);
  for(int i=0; i<N; i++){
    int x_i, y_i;
    cin >> x_i >> y_i;
    x.at(x_i)++; y.at(y_i)++;
  }
  int Q;
  cin >> Q;
  for(int j=0; j<Q; j++){
    
  }
}